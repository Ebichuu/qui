# C05：RSS 与网页独立发现

在 qui 后台运行独立来源观察器，逐项保存发现记录。RSS 按完整条目输出；CHD 网页在当前页解析后输出，后续页失败不回滚前页结果。一个来源的慢响应、失败和退避不阻塞其他来源。同站请求预算限制请求启动频率，不把整个网络响应放进互斥区；重复站点配置按同一源地址共用较严格的预算。

## 适配范围

- 通用 RSS 2.0：标题、显式大小、下载及详情地址、发布时间、显式报告的 infohash 和下载计费因子。GUID 仅作为来源标识，不能冒充真实 hash。
- CHDBits RSS：保留上述字段，并识别已知官种标题后缀。
- CHDBits 网页：普通列表最多五页，保留完整标题提示、列表官种标识、免费标记及标记内部的到期时间；复活列表保留独立事件。复活时间仅按该站七天免费约定推导，缺少证据时保持未知。
- M-Team RSS：从详情路径读取站内 ID；免费状态仅采用来源显式提供的计费因子，不猜测缺失字段。

三类标签独立保留真、假、未知。未知复活时间的记录不可进入有效候选；后续时间明确的记录可进入，跨来源事件归并与规则选择在 C06 处理。来源内键与站点 ID 一起保存，数字 ID 相同的不同站点不会覆盖彼此。未登记的网页适配器不发起请求。

能力登记明确标为 `synthetic_fixtures`：已建立两个实际站点格式的合成适配夹具，不表示已经登录这两个实际站点验证。现场凭据、实际页面差异及当前启用来源仍需后续核实。

## 保存与恢复

SQLite `097_racing_discovery.sql`、PostgreSQL `098_racing_discovery.sql` 保存来源适配器、页数、初始回看秒数、持久发现基线和逐项观察记录。基线在首次有效抓取前建立；初始回看默认零，修改配置和重启不回退基线。旧条目及缺少事件时间的条目仍可观察，但不具有候选资格。规则的接收窗口另行判断，不能用初始回看代替。

首次发现时间保留，重复看到同一来源事件只更新最后看到的时间；公开证据变化时增加版本。每项独立提交，停用或修改来源/站点后，旧抓取不能越过配置版本检查继续写入或更新成功时间。关闭服务会取消并等待全部来源任务。

配置中的凭据和来源地址继续加密。发现记录的下载、详情地址使用独立用途的加密载荷；API 只返回公开字段。Cookie 只发给对应站点源地址，禁止跨源重定向；响应大小和请求时间有界，错误状态不带私密 URL。单个密文损坏只影响对应来源。

`GET /api/racing/discoveries` 返回最新 100 项、各来源状态和能力登记。该接口沿用认证；不创建新页面。C07 再接入原 RSS 页面的来源与规则配置。整个 C05 没有下载器添加、删除、设置修改或种子文件批量下载权限。

## 验证

测试覆盖两数据库的加密、重复观察、补充证据、来源分别记录、稳定基线与旧配置拒写；解析覆盖长标题、独立标签、复活事件、缺测和登录页；HTTP 夹具覆盖慢响应隔离、逐页提交、跨源凭据保护、响应限制及取消。

`make smoke-racing` 扩展到真实程序：慢 RSS 与快网页并行，后页阻塞时前页已能查询；两站格式产生发现，旧条目保持不可用；重启后保留首次发现与身份。下载器写操作始终为零。完整 Go 测试以 `-race -count=1` 并启用 PostgreSQL 通过；最终改动另跑来源与持久化定向测试。`make precommit`、`make test-openapi`（224 个接口）、`make build`、`make smoke-baseline smoke-racing` 通过。前端未改动，本批不重复前端单元测试，构建与现有 58 条警告检查通过；CI 仍执行完整前端测试。

验收脚本现显式持久化随机 `sessionSecret`：缺少该字段的手写测试配置会在每次启动生成不同默认密钥，之前仅比较配置快照未覆盖实际解密。本批增加了重启后实际抓取和更新发现记录的检查。另修复旧下载器慢响应测试在请求取消后偶发返回空 HTTP 200 的夹具竞态。
