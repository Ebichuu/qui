# C02：既有定制恢复

以旧定制 `bfe94b5e` 的净改动迁移到 v1.28.0 底座，保留上游新增功能和修复。未更换模块名、许可证或上游标识。

## 已恢复

- 种子平均上传速度列、排序和自动化条件；按已上传字节 / 活跃秒数计算，缺少活跃时间不匹配条件，列表显示未知值并排在有效值后。
- 删除条件持续时间和 FREE_SPACE 冷却观察；界面以分钟编辑，接口保存秒数。规则停用、删除、修改、任务重新添加、计数重置、采集失败、过期缓存及长时间缺测会清除旧资格，重启不补算计时。
- 多规则 × 多目标批量复制；副本保持禁用，保留持续条件，汇报部分失败。
- 每日上传/下载、服务器统计排序、设置持久化和十种语言的界面字段。
- 旧发布流程的镜像仓库小写与 develop 的 latest 标记修正。开发分支 Racing CI 不发布镜像；镜像只通过 GitHub Actions 构建发布。

## 数据库与采样

SQLite 追加 094，PostgreSQL 追加 095。新迁移创建日统计及排序字段，或在完整验证旧定制结构后接管；保留旧迁移文件名和应用时间。空库、上游库、只完成日统计的中间版本和完整定制库都支持重复启动。

日统计消费现有同步缓存，不发起第二次 qB 同步，也不因打开页面连接下载器。使用成功同步时间作为观测时间，重复和乱序样本不累计；时区变化后按真实时间选取基线，可以重新进入已有日期。统计保留 62 天，首次样本建立基线，随后累计差额；计数器清零后累计新计数。跨日间隔的差额计入新样本所在日期，离线期间无法还原精确的逐日流量。当天无记录时返回不可用，不把昨日数据当作今日零值。

## 验证结果

- 两引擎完整数据库测试通过，包含旧数据保持、升级历史保持、不一致结构拒绝、事务故障及断连/中断回滚。
- 模型、自动化、qB 同步和 API 处理器的竞态定向测试通过。覆盖重复/过期/时区变化样本、重启后的计数基线、持续资格重置和冷却期间观察。
- 前端 101 个测试文件、1,068 项测试通过；OpenAPI 校验和十种语言键检查通过。
- `make precommit` 和 `make build` 通过；前端沿用的 58 条 lint 警告无错误。
- 旧定制真实二进制生成合成 SQLite/PostgreSQL 库，新二进制升级并两次登录/重启；统计、排序、规则及旧迁移时间保持，SQLite 完整性和 PostgreSQL 独立备份恢复通过。
- 本地真实程序连接三个模拟下载器：均速按 97.7 KiB/s、4.88 KiB/s、未知值排序；两规则复制到两目标显示四次成功，副本禁用并保留 1.5 分钟条件；每日统计和排序页面显示正常。

测试未连接实际站点、下载器或生产数据库。生产副本升级、真实文件删除及 Tracker 行为仍需后续现场验收。持续计时当前为内存状态，持久恢复与统一回收协调按 C10 实施。
