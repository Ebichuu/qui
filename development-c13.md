# C13：按实例和动作交接及回退

## 已具备的边界

- 接种与官种回收分别按实例授权。官种回收默认关闭；群组、来源、接种开关及预算不隐式接管删除。
- 日常自动删除与官种回收共用实例／hash／代次占用；未知请求阻挡新代次，不通过重启清空。关闭官种回收后不再开始新步骤，已接受的旧步骤仍核实。
- 重汇报监控、自动化和现有手动入口共用每实例的请求间隔与账号约束。这里的交接针对附加重汇报请求，不能关闭 qB 正常协议汇报来模拟单执行者。
- 原添加意图及回收账本保留在原实例，停用来源、移出群组、关闭开关后不会跨机重发。实际操作提交前后的切换存在边界：已提交的请求不能靠关闭开关撤销。

## 现场交接顺序

每次只切换已验证的一个实例、一个动作范围，并记录时间、源提交、应用版本、数据库版本、qB 版本、存储池映射和负责人。

1. 先在隔离下载器完成该范围的合成与真实站点验收，确认文件访问、Tracker 账号条件和单位。至少两个真实站点的正常间隔、已知等待和错误隔离分别留证。
2. 保存 qui 配置、加密密钥与数据库的一致性备份；qB 的 torrent/fastresume 备份与数据库备份分别保存。新数据目录不能直接覆盖旧数据目录。备份应先在隔离环境实际恢复验证。
3. 暂停原执行者的对应新增动作，例如旧 Vertex 的自动添加／自动删除／附加重汇报。不要只关闭页面，也不要把停止容器直接视为所有远端请求已停止。
4. 核对原执行者、qui 持久操作记录和实际 qB 状态。任何提交中、未知或尚未确认的请求继续由原记录核实；在结果无法确定时，不启动另一执行者的同范围动作。任务消失不能单独证明文件释放。
5. 开启新执行者对应开关，保持其他尚未交接动作关闭。记录第一个操作的提交、qB 结果、任务身份和空间核实；从小范围开始，核对无重复发送与预算变化。
6. 记录旧执行者已停用的证据和新执行者成功结果。没有这两类证据，不把配置开关保存成功称为完成交接。

## 回退顺序

- 首先关闭新执行者对应开关，保留进程和数据库，让已接受请求继续核实；未知请求保持阻挡。不要删除意图、扣账、空间基线或 Tracker 间隔记录来恢复“可执行”状态。
- 必须同时核对数据库与远端 qB。恢复旧数据库会丢失备份后已发送动作的记录；因此不能在恢复数据库后立即开启旧执行者。先保留当前数据库副本，并在隔离环境对账备份后发生的添加、删除和汇报。
- 只有在旧执行者对应范围已无未决冲突、文件与容量状态明确后，才恢复它的新增动作。不能同时恢复新旧两套自动化。
- 应用版本回退必须匹配已验证的数据库备份与配置／密钥。仅切换镜像标签不是已验证回退；本项目镜像只允许通过 GitHub Actions 构建发布。

## 当前证据与未完成项

本地已验证开关默认值、保存／关闭、未知操作持续阻挡、重启不重发及关闭后继续核实。隔离 SQLite 使用在线一致性备份、终止进程、恢复备份并保留同一 qB 远端，accepted 和 unknown 删除意图均保留占用且没有重发。此演练保持了原密钥与配置，不能证明任意旧备份可直接接管。原生文件删除及重启核实已通过开发分支 Actions 的独立 ext4 环境；新增备份恢复版本继续由 Actions 验证。

尚无本任务提供的两个真实站点和隔离下载器资料；未操作现用 Vertex、旧 qui 或 qB 的自动化设置，未执行生产数据库恢复。真实交接、生产副本恢复与回退演练仍待现场验收，不能称 C13 完成。
