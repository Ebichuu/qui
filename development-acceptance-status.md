# Racing 剩余验收跟踪

2026-09-22 核对。开发检查和合成下载器结果不能替代实际站点、实际 qB 或执行权交接的证据。批次细节见根目录 `DEVELOPMENT.md`、`development-c09.md`—`development-c14.md`。

| 项目 | 当前边界 | 下一步证据 |
|---|---|---|
| 复活来源接通（R04） | 表单检查选中来源及站点是否启用、适配器是否支持；停用来源不再隐藏告警 | 实际来源识别、目标不可用及来源恢复的完整配置流程 |
| 连续选机（R19） | 已有实时负载、活动任务、空间和最近预留排序；最近预留仅在前面比较相等时参与 | 连续近似负载场景的分配历史与选择理由，以及明显繁忙或空间不足时的优先约束；不能把精确平局轮换当作长期公平性全部完成 |
| 榜单（R24） | 未实现站点榜单适配，接口明确为 unsupported | 确认目标站点、榜单身份和时间口径，取得无凭据的合成响应样本，再实现旧快照、登录失败、未采到及覆盖率记录 |
| 关键记录（R32） | 有持久接种记录；添加请求结果及身份确认已在默认级别记录站点、实例和 hash，候选评估仍为 Debug | 补齐候选关键状态变化与名称查询；当前接种日志以 hash 关联，正常确认后同步不重复输出 |
| 决策回放（R33） | 添加意图保存原规则和配置版本；候选选择记录会随重新评估更新 | 证明每次实际分配的当时实例/容量及候选依据可恢复，后来的 Peer/ASN/排名不得改写原始证据 |
| 完整替代（R36） | Peer/ASN 已有开发验证；真实竞速收益未验证 | 当前使用功能逐项对照、两个真实站点、真实 qB、相同负载窗口的上传/排名/缺测报告 |

## 开发验证基线

提交 `15b4f9a4` 的 [Racing CI](https://github.com/Ebichuu/qui/actions/runs/35699858148) 于 2026-09-22 全部通过，覆盖完整 Go/前端测试、Linux 原生分配与回收、重启和备份恢复、Peer 历史及共享重新汇报入口。该结果对应此提交，不自动覆盖后续修改。

提交 `5ef0806d` 的 [Racing CI](https://github.com/Ebichuu/qui/actions/runs/35738117168) 全部通过，复活来源启用判断修复已有远端验证。

## 2026-09-26 关键接种日志补充

添加返回结果和成功身份核实使用 Info 记录，携带候选键、站点、实例和已验证 hash；返回结果另标记持久化是否成功，不记录原始网络错误或私密 Tracker URL。已确认任务的周期同步不重复写身份核实日志。名称查询及候选状态变化日志仍待补齐，R32 未全部完成。

`make precommit`、`go test -race -count=1 ./internal/services/racing`、`make build`、`make smoke-racing-execution` 通过。真实进程的合成添加、暂停到传输、重启场景输出 `PASS C08`，验证默认级别的两条关键日志各出现一次且包含 hash、站点及实例，不包含私密 Tracker 地址。保留既有 58 条前端 lint 警告。未改 API 或数据库，本批不重复 OpenAPI 和全量 Go 测试；全量测试由推送后的 CI 执行。用户文档已补日志含义和边界。

## 现场依赖

- 两个目标站点及正常间隔、已知等待和错误隔离证据；账号与密钥不放入公开文档或提交。
- 隔离下载器的 qB 版本、存储池映射和真实文件布局。
- 生产数据库副本在隔离环境的升级与恢复结果。
- 原执行者按实例、动作范围停用的证据，在途操作对账及回退演练。
- 实际离线 ASN 数据库的版本与查询结果核对。测试生成的 MMDB 只验证读取和恢复逻辑。

镜像仅通过 GitHub Actions 构建发布。Linux ext4 合成端到端通过也不代表真实部署布局已经验收。
