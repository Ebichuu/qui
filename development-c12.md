# C12：多站点汇报与删除前约束

## 第一批：重试结果核实

- 替换依赖中可能在耗尽后返回 nil 的重试循环。HTTP 接受不等于 Tracker 成功；原始启用 Tracker 集合必须全部仍在、状态正常且没有未注册消息，才记作成功。
- 查询和等待支持上下文取消；正在更新或等待首次联系时仅观察。部分目标成功后停止整颗种子的重复请求，清单变化或结果缺失不推断成功。完整 URL 仅用于内存身份比较，不新增持久化或日志输出。
- 现有“任意健康 Tracker 则不启动作业”的准入保留，尚未改为逐站点调度。此次只修正已启动作业的结果，不宣称站点账号已入账。
- 待完成：逐站点持久观察、账号适用的已知等待、跨自动化／通用／代理入口共享约束、删除保护，以及至少两个真实站点验收。C11 仍不发送回收删除。
- 验证：`make precommit`、`make build`、reannounce 包 `-race -count=1` 通过。`python3 scripts/smoke-reannounce-result.py` 启动真实应用，以两个合成 Tracker 验证 HTTP 接受后仍失败、部分成功的活动结果及每轮只发送一次；已加入 Actions。没有 API 合约变更或新增页面，因此本批无需 OpenAPI 及视觉检查。

## 第二批：逐 Tracker 历史观察

- SQLite 106／PostgreSQL 107 保存重汇报作业最近查询的任务代次、qB Tracker 观察时间、本地上传计数及其独立同步时间；每任务保留最新快照，写入时清理本实例超过 30 天的历史。旧时间不能覆盖新记录。
- 仅记录 HTTP(S)／UDP Tracker；完整 URL 和消息保存 SHA-256 摘要，保留主机与保守状态分类，不保存 passkey。未知错误不套用站点等待规则，工作状态不等于收到新的 announce 回执或站点入账。
- 查询前后核对真实同步中的任务代次及计数器回退；共享快照过期时按 hash 读取单个任务，不另起全量轮询。任务缺失、存储失败或代次变化终止该次作业。两个样本之间未被观测的变化仍可能不可见，不能把快照当连续事件记录。
- 新增只读 `/api/instances/{instanceID}/reannounce/observations`，最多返回 100 个任务的历史快照。仅覆盖进入重汇报作业的任务，不声称全量 Tracker 覆盖；共享调用约束与删除保护仍待接入。
- 验证：双数据库持久化与旧时间拒绝、reannounce／API handlers 竞态测试、OpenAPI、`make precommit`、完整构建通过。合成真实进程验证逐站点状态和关闭监控后的重启保留；实跑发现的过期共享快照阻断已增加单任务按需读取。真实站点账号和删除保护尚未验收。

## 第三批：统一现有监控范围的请求入口

- 自动化 BulkAction、通用 BulkAction 和代理重汇报共用 dispatch。匹配现有实例监控范围的任务沿用初始等待、健康／待响应判断和作业去重；未匹配范围仍保留原直发行为。错误不走直发回退。
- SQLite 107／PostgreSQL 108 在发送前原子记录实例／hash 的等待期限。原间隔与当前间隔同时约束；重启或缩短配置不撤销原期限。作业使用单次请求客户端，网络结果未知也保留间隔。
- 发送前读取当前设置，复核任务代次与上传计数回退。自动化活动的成功表示请求已处理，新增 phase 明确该语义，不当作 Tracker 接受证明。
- 本批只统一已有监控范围，尚非逐站点账号配置。C11 删除保护、已知站点等待和 C13 操作权交接仍待完成。
- 已通过双数据库并发间隔测试、代理无回退测试及合成进程联动：三入口初始等待、合并单次发送、缩短配置并重启后的期限保护、排除范围继续直发。首次完整回归与前端重建重叠，嵌入资源暂缺导致的编译失败已中止；随后在构建稳定后重新执行完整回归。
- 完整回归另发现 5 个既有 cross-seed 测试受 macOS `/var`→`/private/var` 临时目录别名影响；隔离的 `66294de7` 基线同样复现。以 `TMPDIR` 的真实路径运行后，这 5 个测试全部通过。本批未修改 cross-seed 代码。
- 设置真实路径 `TMPDIR` 后，完整 `make test`（`-race -count=1`，启用 PostgreSQL）全部通过；`make precommit`、完整构建、重试结果／历史恢复与共用入口的合成进程验证通过。保留既有前端 58 条 warning、0 error。
- 十语言的重汇报反馈改为“请求已处理”，预览改为“将请求”，不暗示 Tracker 接受。翻译检查通过，操作反馈相关 23 个前端测试通过；仅调整文字，未新增布局或页面。


## 第四批：精确账号策略与删除前检查

- SQLite 108／PostgreSQL 109 持久化实例＋完整 Tracker URL 摘要＋站点绑定及逐任务代次的已知等待。未知账号不能借同域名绑定获得权限；站点停用或域名移除会使绑定失效。新增开发 API 和 OpenAPI 契约，尚无配置界面或策略删除接口。
- 已知错误仅按明确配置的消息摘要与秒数匹配；同次发送内重复观察不滑动延长期限，新消息或新发送可触发新等待。修改配置不能缩短已记录的等待和发送间隔。发送前刷新单任务与 Tracker 并重新读取策略；不能原子消除外部 qB 修改与发送之间的窗口。
- 范围外任务若命中账号策略，也必须通过完整 Tracker 覆盖与等待检查。正常、待响应、禁用或缺失的 Tracker 阻止整颗种子的重汇报；尚非逐站点独立发送。
- 统一自动删除入口接入当前保护检查。默认 blocked 和 accounted 均不放行；明确 reported_working 仅接受 qB 当前状态，不代表站点入账。日常未管理任务保留既有行为；官种评估要求完整覆盖，合格后只进入 awaiting_execution_plan，仍不执行回收删除。
- 双站点验证均为合成环境，不替代真实账号验收。C11 持久执行计划和空间释放核实、C12 配置界面及现场验收、C13/C14 仍未完成。
- 验证：两引擎账号绑定与等待持久化测试、重汇报定向竞态测试、OpenAPI、完整 `make test`（PostgreSQL 启用，macOS 临时目录取真实路径）、`make precommit` 均通过。既有前端 58 条 warning、0 error；本批未改页面，无新增视觉验收。完整构建及真实隔离进程运行合成账号约束、共用入口和重试核实脚本；真实站点及物理删除仍待现场验证。
