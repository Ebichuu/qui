# C06：来源身份合并与完整规则选择

同站 ID 与首发/复活事件共同确定候选身份；跨站同名或同 hash 保留各站身份。缺少站内 ID 时仅在单来源范围内记录，不能按标题跨来源合并。多路证据保留真、假、未知，矛盾证据不会被后来缺测覆盖；网页显示大小明确标为近似值。候选保留首次发现时间，删除较早来源、补证据和重启均不重置。

规则按接收类型“或”、其余条件“且”匹配。实际官种优先选择接收官种且真正命中的规则，同类保持配置顺序。选中一条完整规则后才检查目标，不能拼接权限、目标或接收窗口；选中目标不可用时等待，选中窗口到期时退出，不自动降到较低规则。未执行动作的观察结果随配置重新评估。官种身份未知时等待证据，不归入普通组。

## 持久化与按需补查

SQLite `098_racing_candidates.sql`、PostgreSQL `099_racing_candidates.sql` 保存候选、规则结果、待评估版本和加密元数据。逐项发现后立即评估；失败仍保留待评估版本，不截断来源其余条目。每轮恢复扫描固定观察上界，避免不断新增条目使早期更新漏评。单事件互斥，其他事件可独立推进；配置变化和到期时间触发重新评估。

必要大小、标题或 hash 证据不足时，仅对该候选获取种子元数据。两名后台工作者和有界队列共享站点预算；慢元数据不阻塞其他已具备证据的候选。真实 hash 基于原始 info 字节计算；校验大小与文件结构，原始文件加密保存，API 仅返回公开名称、大小和 hash。来源报告的冲突不会因拿到一个文件就被抹掉。重启后复用已验证证据。

元数据不能证明官种、免费或站内 ID；当前适配器不能可靠补全的证据继续保持等待。实际站点登录和页面兼容性仍待现场验证。C06 只判断配置目标是否存在且可用，不代表下载器容量已满足；资源承诺、分配、执行中动作冻结和添加结果核实属于 C08。

## 接口与验证

认证接口 `GET /api/racing/candidates` 返回最多 100 项及分页游标，包括合并证据、选中规则、缺失字段与等待原因，不公开下载地址、详情地址、Cookie 或原始种子文件。

两数据库定向测试覆盖事件恢复、版本确认、并发更新、稳定首次发现时间、元数据加密及配置拒写。规则测试覆盖独立三态、类型“或”、官种优先、稳定排序、完整规则、目标不可用、窗口到期和未知证据。慢元数据测试验证另一候选先推进，并验证重启复用。

全量 Go 测试以 `-race -count=1` 并启用 PostgreSQL 通过；最后修改另跑 racing、sources、models 定向回归。`make precommit`、`make test-openapi`（225 个接口）、`make build` 通过。`make smoke-baseline smoke-racing` 启动实际程序，验证双来源事件合并、官种优先、停用后完整规则重选、重启保留身份/时间，下载器写操作为零。前端未改动，不重复前端单元测试；构建与既有 58 条前端警告检查通过，CI 执行全量前端测试。
