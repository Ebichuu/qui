# C09：本机故障联调与性能记录

日期：2026-09-11。基于 C08 `11ac1a5e`。使用独立临时 SQLite、真实 qui 进程、回环 HTTP 来源及下载器，不访问实际站点或现有部署。

## 试跑前登记

固定 12 个合成官种，每个 1 MiB；站点请求起始间隔 1 秒，RSS 周期 10 秒，元数据立即返回，模拟添加响应延迟 50 毫秒；一个下载器，添加并发 2、运行名额 50，共享缓存先预热。使用阶段时间戳按最近秩计算 P50/P95，阈值先于首次执行登记：

| 阶段 | P95 上限 |
|---|---|
| 合成来源发布 → 首次发现 | 15 秒 |
| 首次发现 → 元数据响应 | 35 秒 |
| 元数据响应 → 持久提交 | 10 秒 |
| 持久提交 → 任务确认 | 15 秒 |
| 合成来源发布 → 任务确认 | 60 秒 |

源端等待包括轮询和站点预算；元数据阶段包括排队及站点请求预算。内部时间从模拟服务返回元数据开始，包含验证、调度和提交。确认时间包括 qB 请求、共享同步及 Tracker 核实。失败、未决及重复添加均必须为 0。这些上限用于识别 CI 阻塞，不是生产延迟承诺，也不是与 Vertex 的性能对照。

故障场景使用事件闩锁持续挂起响应，断言另一条完整链路在释放闩锁前完成；不能仅凭最终成功声称互不阻塞。另验证同盘 70+70 MiB 争抢 100 MiB、独立盘推进，以及 reserved/submitted/unknown 三个边界强制结束进程后保持原目标和空间承诺。

## 结果

联调已暴露并修复两处问题：重复发现相同内容会更新候选版本并让预留反复失效；底层 qB 客户端会自动重试断线的添加请求。修复分别保留未改变证据的版本、为接种复用现有连接与认证但仅发送一次。性能试跑还发现已确认任务占用网络名额、新受理请求按固定 5 秒等待确认。改为已确认任务直接消费共享快照、新受理请求每秒核实后，内部等待 P95 从 26.583 秒降至 5.369 秒，保留原 10 秒上限。

通知夹具补入启动基线旧任务后，两个通知 worker 同时阻塞时后续候选仍能添加并确认。首次性能统计修正了 Python 3.9 对 Go 可变精度时间戳的解析；未修改被测阶段或阈值。

独立性能复跑（12 项，失败/未决/重复添加均为 0）：

| 阶段 | P50 | P95 |
|---|---|---|
| 发布 → 发现 | 3.490 秒 | 3.491 秒 |
| 发现 → 元数据 | 6.001 秒 | 13.005 秒 |
| 元数据 → 提交 | 2.369 秒 | 5.369 秒 |
| 提交 → 确认 | 1.273 秒 | 3.487 秒 |
| 发布 → 确认 | 12.669 秒 | 23.347 秒 |

最终完整运行 `make smoke-baseline smoke-racing-execution smoke-racing-integration` 全部通过：空库启动/认证/重启、默认停用、暂停/传输阶段、双来源两种先后、慢页/元数据失败隔离、共盘争抢、慢同步隔离、三个崩溃边界、添加超时、双通知 worker 阻塞及性能采样。该轮内部等待 P95 为 6.545 秒，总耗时 P95 为 24.672 秒，仍满足原阈值。

`make precommit`、`make build` 通过；前端保留既有 58 条警告，无错误。`go test -race -count=1` 覆盖 models（SQLite/PostgreSQL）、racing、qbittorrent，最后调度调整另跑 racing 全包通过。本批未改 API 契约和前端组件，未重跑 OpenAPI/全量前端测试；推送后由 Racing CI 执行完整 Go/前端回归及三组进程验证。文档已同步更新。镜像未在本地构建或发布。

## 现场验收边界

尚未提供两个实际站点、现用 Vertex 同配置/同负载的可比数据、生产数据库副本与真实磁盘布局，因此真实站点接种试跑及性能对照仍未完成。Q2 本机通过不能替代上述现场验收；不执行新的回收、删除或汇报交接。
