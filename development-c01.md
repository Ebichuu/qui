# C01 数据库升级衔接

日期：2026-09-10。范围：旧定制数据库的来源识别、结构校验、历史保留、失败和中断恢复；生产副本升级与线上切换尚未执行。

## 实现

迁移记录的键是完整文件名，同编号不表示相同迁移。保留旧定制 daily stats / server sort 文件名和原应用时间，不增加改名映射；上游 partial pools、触发器及索引清理仍按独立迁移执行。C01 不新增业务表；C02 随定制功能恢复补齐新安装所需迁移和模型。

SQLite 在历史文件名规范化与业务 DDL 前校验，PostgreSQL 在已有迁移锁及事务内校验。检查表/记录对应、完整字段、类型、默认值、非空、主键、级联外键和日期索引。未知或不一致状态明确停止，避免用记录掩盖缺失数据。支持只完成旧日统计迁移的中间版本。

新增合成数据库测试使用旧提交 `bfe94b5e` 的四份原始定制 SQL；经逐字核对，共有的 SQLite 001—090 与 PostgreSQL 063—092 迁移与当前上游一致。测试不依赖旧本机工作区，也不需要生产数据。

## 验证与限制

验证包括两种引擎的空库、上游旧库、定制中间版本/完整版本、超过 32 位的统计数值、历史规则和排序保持、原迁移时间保持、重复启动、缺表/缺记录/缺列/错误类型/默认值/非空/主外键/索引拒绝、迁移批次失败回滚。SQLite 子进程在实际迁移事务内被终止；PostgreSQL 迁移连接被终止，随后重新启动。

本机额外使用从 `bfe94b5e` 源码构建的旧程序创建合成 SQLite 库，再运行 C01 程序两次完成登录和数据核对；日统计、排序、规则及旧迁移时间保持，新增上游表出现，完整性检查返回 `ok`，升级前备份仍可读取且内容一致。该验证没有连接实际下载器。

| 检查 | 结果 |
|---|---|
| `QUI_TEST_POSTGRES_DSN=... go test -race -count=1 -timeout 10m ./internal/database` | 两引擎数据库包通过，238.136 秒 |
| 新增 PostgreSQL 连接中断定向用例 | `-race -count=1` 通过，2.150 秒 |
| `make precommit` | 通过；前端 58 条上游既有警告、0 错误 |
| `make build` | 前后端构建通过 |
| 旧 SQLite 二进制 → 新二进制 → 再启动 | 登录、统计/规则/排序/迁移记录及完整性检查通过；备份内容保持 |
| 旧 PostgreSQL 二进制 → 新二进制 → 再启动 | 两次登录、统计/排序/迁移记录保持，上游表出现；`pg_dump` 在独立数据库实际恢复并逐项比较通过 |

本地完整验证日志留在 Git 忽略的 `.dev/database-c01.log`、`.dev/c01-disconnect.log`、`.dev/precommit-c01.log`、`.dev/build-c01.log`、`.dev/live-c01.log`、`.dev/live-c01-postgres.log`。测试 PostgreSQL 为独立 16.15 实例，未使用生产连接。前一批 C00 的 GitHub Actions `34397492033` 已成功完成完整 Go/前端测试、构建及启动验证。

未做生产副本与真实站点验证，尚未交接自动操作。完整 Go/前端套件由本次 GitHub Actions 继续验证，本地只扩大到实际修改的数据库包。

本批增加 Racing CI 的 PostgreSQL 服务，使数据库用例持续覆盖两个引擎。备份和回退步骤见 [定制数据库升级](documentation/docs/configuration/custom-dashboard-upgrade.md)。本批没有 API、前端文案或生产 schema 新增，不需新增 OpenAPI/i18n 契约；日统计及排序界面尚未恢复，属于 C02。
