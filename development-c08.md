# C08：选机、空间预留与可靠添加

日期：2026-09-11。基于 C07 的统一来源、完整规则选择和共享状态缓存推进。验证仅使用本机合成来源、模拟下载器和独立数据库；没有接管现有部署。

## 实现

- SQLite 099 / PostgreSQL 100：实例接种开关、持久添加意图、各阶段时间、共享磁盘承诺、配置版本和元数据来源版本。升级后接种默认关闭。
- 全部配置修改与发送边界共用短数据库锁。提交前核对配置、候选证据版本、未评估的新发现、窗口、实例启用状态和容量。网络请求不在事务中执行。
- 官种跨分页优先，同类按首次发现时间调度。完整命中规则确定一个目标，群组不可用时不会借另一规则的目标。实例按健康、新鲜度、路径、容量、名额筛选后比较当前负载与最近分配。
- 磁盘余量与任务完成量从同一份共享 MainData 缓存投影。跨组统一预留，同盘余量不相加；暂停和排队任务仍计未来写入。临时/最终目录跨盘分别预算，容量未知则等待。
- 先提交持久意图再调用 qB；每实例独立并发。请求超时或发送边界重启后仅核实原目标，不重复添加。旧执行任务不能提交或取消已经重新生成的预留。
- 任务 hash 与完整账号 Tracker 地址核实后才确认；接口受理、任务确认、可运行和首次传输分别记录。同 hash 不会自动转到另一实例制造额外参与副本，也不会擅自修改 Tracker。
- 已确认任务从新鲜快照连续消失至少 30 秒才释放预留；超过 10 秒的观测间断重新计时。原事件历史保留，独立复活事件不被永久 hash 历史去重吞掉。未知提交不采用这条释放路径。
- 设置 → 下载器加入接种开关、并发、名额、保留空间、路径/分类与暂停参数。RSS → 规则加入阶段记录。十种语言覆盖，开启提示明确旧自动添加需要先停止。
- 添加通知沿用已有异步队列；没有另建通知等待链。镜像仍仅允许 GitHub Actions 构建发布，本批不发布镜像。

用户说明见 [下载器接种](documentation/docs/features/racing-reception.md)。

## 验证

| 范围 | 结果 |
|---|---|
| SQLite + PostgreSQL | 同盘 70/70 争抢 100 仅一个成功、多盘原子回滚、重启/未知预留、配置与证据失效、旧执行任务代次、移除与复活通过 |
| 执行器 | 慢实例隔离、超时不重发、暂停与传输阶段、不同账号 Tracker 拦截、配置关闭通过 |
| 缓存与选机 | 不额外刷新、剩余文件量不使用下载流量、共盘聚合、异盘完整复制、过期隔离、禁止意外跨实例重复通过 |
| 完整 Go 回归 | `go test -race -count=1 -timeout 20m ./...`，包含 PostgreSQL，通过；最后局部修正另跑相关测试 |
| 完整前端 | 106 个文件、1080 项测试通过；使用 2 个 worker，避免并行构建/全量测试争抢资源造成断言超时 |
| 格式与检查 | `make precommit` 通过；前端保留原有 58 个警告，无错误 |
| 接口与语言 | `make test-openapi` 229 个接口通过；`pnpm check:i18n` 无错误 |
| 构建 | `make build` 通过，不构建镜像 |
| 实际进程 | `make smoke-baseline smoke-racing smoke-racing-execution` 通过：默认不添加、验证 metainfo、只添加一次、分阶段确认、停用和重启保持 |
| 中文页面 | 独立程序中查看 RSS 接种阶段，编辑下载器名额并保存启用，验证提示、状态刷新和表单布局 |

## 边界与后续

C09 继续将双来源先后到达、慢元数据、慢通知、同盘争抢和发送边界故障贯通到进程级测试，并记录分阶段 P50/P95。当前没有可据此声称通过的生产性能对照。

至少两个实际站点、生产数据库副本、真实磁盘行为以及与当前 Vertex 同配置/近似负载的对照尚未验证。未决请求没有自动放弃或人工重发入口；不执行新的官种回收、删除或汇报。当前版本仍不能替换现有部署。
