# C07：原 RSS 页面配置与迁移预览

RSS 页面新增管理范围选择，默认打开 qui 统一来源，保留订阅源和规则两个页签。旧下载器 RSS 管理继续按实例运行；带旧订阅或规则定位参数的链接仍进入原实例视图。集中来源不依赖下载器实例，即使没有下载器也可配置站点与来源。

主页面提供站点、RSS/网页/复活来源、三类接收复选框、下载器或群组目标，以及展开的窗口、关键词、大小、官种腾空间允许开关。新增配置默认停用，保存或启用不授予添加/删除权。编辑时私密字段留空保持原值；Cookie 清除有独立控件。群组复用 C04 设置，不在 RSS 页面复制回收条件或预算。

## 规则、预览与旧配置

匹配预览读取 C06 的持久候选与完整规则结果，显示首次发现和缺失证据；页面每五秒查询，关闭页面后采集和评估仍持续。排序通过认证接口 `PUT /api/racing/rules/order` 一次提交全部 ID，事务只更新顺序；缺项、重复或规则集合变化时拒绝，不覆盖其他规则字段。无需新增数据库迁移。

qB 迁移预览复用既有只读 RSS 接口，Vertex 导出在浏览器本地解析。支持单个来源/任务/规则文件与三个数组的集合格式，逐项保留未知设置，不执行 JavaScript 或表达式。路径、分类、暂停、自动管理、正则/剧集/忽略天数等条件均显式列出。Vertex ID 引用、AND/OR 语义、任务级免费检查、Cron/随机间隔与目标映射均标为待核对。

预览不导入、不自动启停原规则，不把不支持项丢掉后继续启用。凭据及 URL 私密部分隐藏，损坏或未识别导出拒绝。实际账号、原任务清单与执行权交接仍待现场核实。中文及英文文案齐全，其余既有语言保持同键覆盖。

原日志入口可查询来源失败与恢复，Debug 级别包含采集耗时、条目数量及评估状态。只记录稳定 ID、状态与固定错误分类，不打印凭据、URL 或标题。

## 验证

定向前端测试覆盖无实例的集中入口、接收类型与窗口必填、完整目标、私密字段保留/清除、qB 参数完整预览、Vertex 语义差异及未知字段/损坏导出。全量前端 105 个文件、1,078 项测试通过。两数据库排序测试验证顺序、字段保持及整次拒绝；models、api、handlers、racing 定向 Go 测试均以 `-race -count=1` 通过，并启用 PostgreSQL。最后日志/路由改动另跑定向回归。

`make precommit`、`make test-openapi`（226 个接口）、`make build`、全部 i18n 检查及 `make smoke-baseline smoke-racing` 通过。既有 58 条前端 lint 警告保留。本批未修改其他后台业务，不再重复 C06 已通过的全量 Go 测试。

隔离真实程序的中文页面完成站点、网页来源、官种或免费规则保存、群组选择、旧 RSS 切换、qB 迁移字段预览。合成 HTTP 来源在后台产生候选，页面显示规则匹配通过和首次发现时间。实际程序 smoke 另验证规则顺序接口、失败时保持原顺序、双来源、重启与下载器写操作为零。仅使用合成站点和下载器，尚未验证实际站点登录或生产库副本。

## 2026-09-22 复活来源可用性补充

规则编辑页原来只检查适配器能力，已停用的来源或站点也会隐藏复活入口告警。现同时检查来源与所属站点的启用状态；其他已选来源仍可独立满足条件，不因一条停用来源误报。沿用原提示位置和保存行为，十语种提示明确检查入口。

7 项 RSS 表单测试通过，覆盖来源停用、站点停用、正常启用以及混合来源选择。`make precommit`、`make build` 通过，保留已有 lint 警告；使用真实 qui 进程和合成配置在 Chrome 中文页面确认停用来源的提示可见。Docusaurus 已补说明。此补充不等于 R04 全项或真实站点已验收。
