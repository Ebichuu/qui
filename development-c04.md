# C04：共享状态、下载器群组与存储池

复用现有每实例同步循环，加入应用生命周期的后台消费者。页面关闭后仍同步；网页订阅加入或退出不会建立另一套轮询，也不会为后台物化种子列表。实例配置每五秒核对，慢实例的超时及退避只影响自身。原 SSE 与竞速观察共用通知分发。

## 状态与空间

竞速读取共享缓存的轻量视图，不创建客户端、不触网、不复制全部种子。记录 MainData、偏好和版本各自的观测时间；MainData 超过五秒、最近同步失败或实例不健康时标记陈旧。偏好异步刷新，每实例最多一项，慢请求不会阻塞 MainData；取消后不再继续请求版本信息。

存储池表示一个物理空间范围，与下载器群组分开。每实例可映射多个绝对路径，支持 POSIX、Windows 盘符及 UNC；嵌套挂载按最长路径匹配。路径映射与池持久化在 SQLite `096_racing_storage.sql` 和 PostgreSQL `097_racing_storage.sql`。

qB 的默认目录剩余空间仅用于该目录映射的存储池。目录偏好必须新鲜，且剩余空间样本不早于路径观测。多个下载器报告同池时取当前较小值，不相加；另一块盘、未知路径或陈旧数据不生成可用容量。未来写入承诺和预留在 C08 接入。

## 页面与验证

在原“设置 → 实例”页面加入群组、存储池和路径映射。群组只需名称、启用和成员多选，支持空组及跨组成员重合；引用保护继续适用。观察状态及时间与版本、实际保存路径一起显示。没有新增任务页面，也不执行新增自动接种或回收。

已验证：两数据库的存储模型、稳定 ID、改名、重复路径拒绝、失败回滚和引用删除保护；共享循环与网页订阅共存、网页关闭后保留循环；缓存无网络读取、过期和失败失效；同盘不重复相加、嵌套磁盘及路径变更不借用旧容量。群组页面测试覆盖改名停用与成员变更、空组以及删除失败保留提示。

扩展 `make smoke-racing`，使用两个本地模拟下载器检查无人打开页面时仍持续同步；其中一台延迟十五秒，另一台仍更新。配置重启恢复，来源请求和下载器添加、删除、设置修改均为零。本地实际页面完成新建双成员群组、中文布局及共享磁盘容量显示检查。未连接真实下载器或站点。

完整 Go 测试以 `-race -count=1` 并启用 PostgreSQL 通过，前端 102 个文件、1071 个测试通过；`make precommit`、OpenAPI 和十种语言检查通过。macOS 的全量文件测试使用解析后的 `TMPDIR`，避免系统符号链接干扰路径边界夹具。

公开说明见 [下载器群组与存储池](documentation/docs/features/racing-groups.md)。C05 将接入 RSS 与网页的独立逐项发现。
