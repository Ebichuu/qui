# C03：最小配置模型与后台生命周期

Q1 新增站点、来源、RSS 规则、群组和成员的持久化模型及认证 API。SQLite 使用 `095_racing_configuration.sql`，PostgreSQL 使用 `096_racing_configuration.sql`。没有提前建立 Q2/Q3 的添加、删除、预算或执行权数据。

## 实现边界

站点持有服务端加密凭据引用；来源地址单独加密，API 只返回协议和主机。来源归属站点，规则稳定引用来源，并选择一个下载器或一个群组。接收类型多选、显式接收窗口、筛选字段、排序及官种回收允许开关保存在同一条规则中。

群组成员引用现有实例，不复制凭据；同组成员去重，跨组成员允许重合。被引用站点、来源、群组和实例有外键保护；组和规则删除时只清理自身关联，来源和站点的秘密记录随其所有者清理。修改失败会回滚整个事务。

后台只依赖配置存储，没有 qB 添加、删除、汇报或来源网络接口。随应用启动加载配置，API 修改以合并通知刷新，短暂数据库错误独立重试，停止时等待后台退出。模式固定为 `observe_only`。页面断开不影响生命周期；此时“启用”只保存配置，不表示已开始抓取或抢种。

## 验证

- SQLite/PostgreSQL 的非空配置创建、读取、改名、停用、重建存储对象后恢复、成员去重、引用拒绝和失败回滚测试。
- 凭据 AES-GCM 实际解密对照、省略字段保留、显式清除，以及响应不含完整来源路径、passkey、Cookie、秘密引用或密文。
- 新增 14 个 API 操作全部检查未登录拒绝；请求字段、输入边界、引用冲突、响应结构及路由/OpenAPI 对应检查。
- 后台启动、配置刷新、短暂错误后自动恢复、请求取消隔离、进程生命周期停止及重新恢复测试。
- `make smoke-racing` 启动真实程序，在临时空库创建合成站点、来源、群组和规则，保存与改名后重启核对；本地来源探针确认 Q1 未发起抓取请求。Racing CI 已加入此项。

全量 Go 检查最初暴露两项已有问题：macOS 临时目录经 `/var` 符号链接进入文件路径测试，改用解析后的 `TMPDIR` 后跨种回归通过；SQLite 全局连接钩子延迟注册与直接打开连接竞争，改为包初始化注册后并发回归通过。数据库、模型、API、后台服务均以 `-race -count=1` 复测通过；`make precommit`、`make build`、OpenAPI 和两项真实程序冒烟检查通过。

公开使用说明见 [竞速配置](documentation/docs/features/racing-configuration.md)。没有连接真实站点或下载器，也没有更改线上配置。群组页面、存储池映射及共享状态订阅属于下一批 C04；来源抓取、规则求值及 RSS 页面接入分别按 C05—C07 推进。
