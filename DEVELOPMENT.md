# 竞速扩展开发计划

## 目标与底座

以 qui v1.28.0 为底座，在同一个 Go＋React 应用中保留既有定制，并实现多站点双来源发现、规则筛选、群组选机、资源预留和可靠接种。随后接入官种主动回收、多站点汇报及异步分析。

代码位于 `Ebichuu/qui` 的 `racing/develop` 分支，现有 `develop` 保留定制版本。基线提交及工具链见 [锁定记录](development-baseline.json)，C00 验证及已知限制见 [启动记录](development-c00.md)。当前不能作为现用版本的替代品。

## 实施顺序

| 批次 | 内容 | 状态 |
|---|---|---|
| C00 | 锁定源码，验证构建、空库启动、登录和重启 | 本地验证通过；现场清单待核实 |
| C01 | SQLite/PostgreSQL 迁移冲突识别、升级衔接和恢复 | 已实现并通过两引擎合成库/真实程序验证；生产副本待验证 |
| C02 | 保留均速、持续条件、冷却观察、批量复制、日统计、排序和发布修正 | 待实施 |
| C03 | 最小站点/来源/规则/群组模型和后台服务生命周期 | 待实施 |
| C04 | 复用 qB 状态同步，加入群组与存储池映射 | 待实施 |
| C05 | RSS 与网页独立发现、逐项输出、站点请求预算 | 待实施 |
| C06 | 来源身份合并、去重、官种优先和完整规则选择 | 待实施 |
| C07 | 沿用 RSS 页面配置来源和规则，提供迁移预览 | 待实施 |
| C08 | 综合选机、共享空间承诺、先登记意图再添加、结果核实及恢复 | 待实施 |
| C09 | 双来源联调、性能对照、小范围接种试跑 | 待实施 |
| C10 | 持续低效观察、下载器回收设置及统一删除入口 | 待实施 |
| C11 | 具体官种缺口触发，预算内逐项回收并核实空间 | 待实施 |
| C12 | 多站点/Tracker 汇报状态及删除前约束 | 待实施 |
| C13 | 按实例和动作交接执行权，演练回退 | 待实施 |
| C14 | Peer/ASN、排名和历史分析，全量替代验收 | 待实施 |

C00—C02 完成可运行的升级底座和定制保留；C03—C07 只观察；C08—C09 交付可接种首版；C10—C13 完成回收及汇报交接；C14 完成分析。新功能不能绕过依赖阶段提前执行自动操作。

## 必须保持的行为

- RSS 与网页互补，谁先发现且必要信息齐备就推进；单页、单个候选、后续接种不等待其他来源、分页、整批元数据或通知。
- 官种、免费、复活各自保留真/假/未知，多选按“或”，其他过滤条件按“且”；实际官种始终优先。同一事件只使用一条完整有效规则，不拼接目标和回收权限。
- 同站事件合并，真实 hash 参与核实；标题不是身份。复活不能被永久历史去重吞掉，跨站身份与账号关系保留。
- 每实例复用现有状态同步，保留 SSE 消费者。状态过期不能当作空闲；群组不是磁盘，同盘容量及未来写入承诺统一核算。
- 本地事务先保存操作意图和资源承诺，再请求 qB。超时或重启后核实原目标，不盲目换机重投；接口成功、任务存在和开始传输分别记录。
- 只有具体官种造成真实缺口，且 RSS 开关、下载器配置、删除权、候选资格、站点条件和预算均满足时才回收。候选不足不先删，删除任务不等于空间已释放。
- 冷却仍继续观察；规则变更、任务代次变化、计数器重置和缺测使旧持续资格失效，重启不补算缺测。少量上传是否低效按持续规则判断。
- 每个实例范围内，每类自动操作仅有一个有效执行者；加入群组或开启 RSS 不自动交接删除权。未知操作及已消耗预算不能通过重试或重启重置。
- 统计、通知和分析有界异步执行，不能挤占接种、状态同步、持续观察和未决操作核实。
- 沿用 RSS、下载器、自动化和日志页面，不另建竞速任务页或微服务；不增加 Redis、消息中间件或保种模式。

## 验证与发布

每批按改动运行定向 `go test -race -count=1`、前端测试、`make precommit` 和 `make build`；契约、文案及数据库变更分别执行对应检查。C01 使用空库、上游旧库和定制旧库，验证两种数据库的数据保持及中断恢复。

接种验收覆盖两路先后到达、慢页/慢元数据/通知阻塞、慢下载器隔离、同盘容量争抢、请求超时但远端成功和重启恢复。正式多站点验收至少覆盖两个实际站点；真实文件释放、Tracker 行为及交接需要现场验证。性能阈值在试跑前登记，再报告可比条件下的 P50/P95、失败和未决数量。

Racing CI 在开发分支推送后执行完整 Go/前端测试、构建和空库启动验证。镜像仅由 GitHub Actions 构建发布；本阶段 CI 不发布镜像，后续发布需记录源提交、上游基线、数据库版本与镜像摘要。
