# C11：官种缺口与预算回收

## 第一批：只读评估

- 仅真实官种事件、胜出规则允许回收且没有直接接收目标时启动；保留目标范围、健康、名额、路径与共享空间判断。独立单槽、15 秒上下文，不占添加与核实名额。
- 空间预算保留负余额，旧超额承诺计入缺口；组合评估按近期上传／可靠容量排序，整体预算不足不返回部分删除列表。数量、总量、近期上传、超额上限受初始及当前较严值约束，纯函数接受已消耗量；窗口定义变化要求重建证据。
- SQLite 104 / PostgreSQL 105 保存事件／实例的历史评估及配置版本；提供最近 100 条只读查询。记录不是执行计划、空间预留或预算消耗账。
- 当前没有物理独占释放及近期上传窗口证据提供者；这些值保持未知，不用逻辑体积和全程上传替代。不发送删除。实际空间足够时仍回到原接种入口。
- 仍需可靠容量／窗口证据、持久计划与累计消耗、逐项核实和 C12 站点保护，不能称 C11 完成。

## 本批验证

- `make precommit`、`make build`、`make test-openapi` 通过，保留既有前端 58 条 warning、0 error。
- racing、models、API handlers 相关整包 `-race -count=1` 通过；模型启用真实 PostgreSQL，评估记录同时验证两引擎配置版本拒绝与持久化。
- `python3 scripts/smoke-reclaim-assessment.py` 在完整构建后两次通过：具体官种缺口持久化、无部分删除、崩溃恢复、真实空闲容量增加后回到正常添加。已加入 GitHub Actions。
- 独立子代理复核完成；本批没有改变站点汇报或实际回收权。新增页面没有改动，因此没有新增视觉验收；合成 API 与进程是本批实跑证据。真实站点和物理删除仍未验收。

## 第二批：真实同步样本与近期上传窗口

- 候选读取不再套用界面乐观状态，使用同一份真实 qB 同步快照及成功同步时间；重复 API 读取不会补计时间。
- SQLite 105 / PostgreSQL 106 保存实例、hash、代次、同步时间及上传计数器，保留配置窗口加最大采样间隙的历史。每次只读回按任务聚合的计数；代次改变、观测到回退、长缺口或任务消失清除该任务窗口。
- 只有窗口起点之前存在连续基线时才返回近期上传字节，使用包含边缘采样区间的保守上界。未知省略字段，不用全程上传代替；C11 评估接收窗口覆盖数据，物理释放仍保持未知。
- 快照不能识别两次观测之间已经恢复到旧值以上的计数器重置，这是采样证据的限制，不宣称连续事件追踪。

- 实跑发现并修复真实快照与界面缓存采用不同过期门槛造成的计时重置；候选统一使用同一真实样本的 5 秒有效期。修复后候选成熟、窗口覆盖、重启与代次重置实跑通过。
- 合成进程 HTTP 客户端显式直连本机，避免系统代理拦截启动检查；一次代理 502 失败后直连复跑通过。

## 第三批：保守的 Linux 容量证据

- Linux amd64/arm64 本地文件读取 FIEMAP 区段，只接受原生 ext4/XFS/Btrfs 的单硬链接普通文件；共享、未知、待分配、编码/压缩、不支持的标志、跨文件系统或扫描中变化保持未知。Overlay 不作为释放证据。
- 安全根目录限制路径；只读检查同盘各实例文件清单与文件身份，发现跨种共享、缺失清单或过期快照不计容量。扫描有上限，不占接种名额。
- macOS、Windows 及不支持的平台返回未知，不回退到逻辑大小。Linux 包已交叉编译，本机不能运行 Linux 专用检查；Linux 实跑待 Actions 验证。
- 即使容量组合可行，历史评估状态仍为等待站点保护，不发送删除，也不声称物理空间已经释放。
- 本批 `make precommit`、完整构建、OpenAPI、相关包竞态测试、双数据库窗口测试及合成进程检查通过。Linux 专用静态检查与交叉编译通过；本机为 macOS，不能以此代替 Linux 文件系统实跑。
